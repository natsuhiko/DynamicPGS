% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpreg2.R
\name{gpreg2}
\alias{gpreg2}
\title{Step 2 optimisation of individual-level dynamic deviation components}
\usage{
gpreg2(
  adata,
  delta2d0 = c(0.94, 1.7),
  Verbose = F,
  Plot = F,
  ncore = max(1, parallel::detectCores() - 1),
  MAXITR = 100
)
}
\arguments{
\item{adata}{A `DynamicPGS` object after running [gpreg1()].}

\item{delta2d0}{Numeric vector of length 2 giving initial values for the
variance components of individual-level dynamic and intercept effects.}

\item{Verbose}{Logical. If `TRUE`, prints detailed optimisation diagnostics.}

\item{Plot}{Logical. If `TRUE`, plots the lower bound and fitted trajectory
during optimisation.}

\item{ncore}{Integer. Number of CPU cores used for parallel computation.}

\item{MAXITR}{Integer. Maximum number of optimisation iterations.}
}
\value{
The input `DynamicPGS` object updated with second-stage estimates,
  including `delta2d`, `lball_d`, and association-mapping intermediate
  quantities generated by [prep_assoc()].
}
\description{
`gpreg2()` fits the second-stage model for individual-specific dynamic
deviations around the population-level trajectory. This step estimates
variance components for dynamic individual effects and prepares the object for
association mapping.
}
\details{
This function requires the first-stage optimisation result from [gpreg1()].
At the end of optimisation, [prep_assoc()] is called automatically to compute
matrices used by [getP()].
}
\examples{
\dontrun{
adata <- getData("phenotype.tsv")
adata <- gpreg1(adata)
adata <- gpreg2(adata, ncore = 4)
}

}
