% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getDoseFromVCF.R
\name{getDoseFromVCF}
\alias{getDoseFromVCF}
\title{Read genotype dosages from an indexed VCF file}
\usage{
getDoseFromVCF(vcf, region, tabix = "tabix", chunk_size = 1000)
}
\arguments{
\item{vcf}{Path to a bgzip-compressed VCF file indexed by tabix.}

\item{region}{Character vector of genomic regions passed to tabix, for example
`"chr1:100000-200000"`. At least one region must be supplied.}

\item{tabix}{Path or command name for the tabix executable.}

\item{chunk_size}{Integer. Number of VCF lines read per chunk.}
}
\value{
A numeric dosage matrix with variants in rows and samples in columns.
  Row names are constructed as `CHROM:POS:REF:ALT`. Variant metadata are stored
  in the `"variant_info"` attribute.
}
\description{
`getDoseFromVCF()` extracts variants from a bgzip-compressed and tabix-indexed
VCF file and returns a dosage matrix. If the VCF contains a `DS` field, dosage
values are read from `DS`. Otherwise, genotypes in the `GT` field are converted
to alternate-allele dosages.
}
\examples{
\dontrun{
Gall <- getDoseFromVCF(
  vcf = "imputed.vcf.gz",
  region = "chr1:100000-200000"
)
attr(Gall, "variant_info")
}

}
