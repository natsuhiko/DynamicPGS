% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpreg1.R
\name{gpreg1}
\alias{gpreg1}
\title{Step 1 optimisation of the population-level Gaussian-process regression model}
\usage{
gpreg1(adata, rho_a = 0.01, rho_b = 5, Verbose = F, Plot = F, MAXITR = 100)
}
\arguments{
\item{adata}{A `DynamicPGS` object returned by [getData()].}

\item{rho_a}{Shape-like hyperparameter for the inverse-gamma prior on `rho`.
Internally scaled by the number of individuals.}

\item{rho_b}{Scale-like hyperparameter for the inverse-gamma prior on `rho`.
Internally scaled by the number of individuals.}

\item{Verbose}{Logical. If `TRUE`, prints optimisation details.}

\item{Plot}{Logical. If `TRUE`, plots the lower bound and fitted trajectory
during optimisation.}

\item{MAXITR}{Integer. Maximum number of optimisation iterations.}
}
\value{
The input `DynamicPGS` object updated with first-stage estimates,
  including `rho`, `delta2`, and `lball`.
}
\description{
`gpreg1()` fits the first-stage Gaussian-process regression model for the
longitudinal phenotype. This step estimates the population-level smooth
trajectory, covariate variance components, the GP length-scale parameter
`rho`, and the residual variance.
}
\details{
This function should be run after [getData()]. The fitted object is required
by [gpreg2()] and downstream association testing.
}
\examples{
\dontrun{
adata <- getData("phenotype.tsv")
adata <- gpreg1(adata, Verbose = TRUE)
}

}
